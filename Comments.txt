Привет!

Какие-либо изменения я вносил только в MainPage.xaml, MainPage.xaml.cs, City.cs
(созданный мною класс) и WeatherPage.xaml (созданная мною новая страница).

В MainPage, по моей задумке, должен быть список городов, и когда я кликаю на
один из них - меня перенаправляет на новую страницу с подробной информацией о
погоде в выбранном городе. Для списка я решил использовать Picker. 

Первый коммит "Added working code": Я добавил рабочий код, где добавил данные в
Picker.Items вручную. Если запустить приложение, то по центру экрана будет
Label, под ним список Picker, выбираешь в Picker`е город - тебя перенаправляет
на новую пустую страницу.

Второй коммит "Some adjustments needed": Моей первой проблемой стала привязка
данных (а именно Name) в Picker из списка cities с экземплярами класса City. Я
считаю, что Picker.Items должны ссылаться на Name каждого города из списка, но
не знаю, как это сделать. Я уверен, что надо использовать атрибуты ItemsSource и
ItemDisplayBinding у Picker, но я не знаю, как именно должно это выглядеть
(перепробовал много вариантов - ничего не подошло).

Workaround: Насколько я понял, привязка ItemSource в XAML предназначена больше
для работы с отдельным классом модели данных (ViewDataModel). Для того, чтобы
использовать коллекцию, созданную в классе, я напрямую присвоил ItemDataSource в
CS.

Вторую проблему я обнаружил, когда попытался привязать текст из выбранного Item
у Picker к Label в новой странице. К примеру, я выбрал город, меня перенаправило
на новую страницу, и там сверху страницы написано название города, который я
выбрал. Программа просто не видит Picker из MainPage, хотя если эту же строку
поместить в этот самый MainPage, то всё заработает.

Fix: Это нормально, так как WeatherPage -- это новая, независимая от MainPage
страница. В соответсвии с принципами объектно-ориентированного программирования,
каждый объект хранит своё состояние независимо от других. В зависимости от того,
что ты хочешь делать в будущем, существуют различные решения. Насколько я понял,
сейчас WeatherPage будет необходим только один конкретно выбранный город.
Поэтому я добавил в конктруктор WeatherPage параметр типа City, куда и передал
выбранный город. Так как моей целью было тебя "разблокировать", то пока что я
просто присвоил имя города заголовку страницы. Тебе, скорей всего, нужно будет
завести поле "город" в классе и сохранить туда переданный в конструктор город,
чтобы использовать все его поля.

Это все с учетом того, что по планам я должен использовать API и брать данные с
openweathermap.org - там у меня другие проблемы и недопонимания, но я над ними
думал не особо долго, потому что стоило бы сначала разобраться с этой версией
приложения.

Также стоит отметить, что вид приложения на Android и iOS различается.

Заранее спасибо :)

08.07.2020

Преобразил ContentPage в NavigationPage и решил, что нужен изменяющийся Title, 
в зависимости от того, какой город я выбрал.

Перепробовал кучу способов, один из них я оставил, чтоб ты посмотрел, в каком
направлении я двигался.

Помоги, пожалуйста, сделать нужный мне Title, а после этого я буду плавно
переходить к использованию API и пытаться доводить программу до конечного ее
вида. Спасибо)

17.07.2020
Слишком сложно. Binding нужен, когда на твоей странице что-то динамически
меняется, и ты хочешь это отобразить автоматически. В случае WeatherPage, для
выбранного города создаётся НОВЫЙ объект WeatherPage (оператор new), который
соответствует городу. Для такой страницы Title не будет изменяться, и можно
напрямую его присвоить в конструкторе. (Вдобавок, свойство TitleProperty только
на чтение).
